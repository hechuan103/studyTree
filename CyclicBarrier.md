**CyclicBarrier**

CyclicBarrier是一个同步辅助类，它允许一组线程等待到彼此都到达一个共同的屏障点。CyclicBarrier在涉及固定大小线程池的程序中偶尔需要等待彼此时很有用。屏障被称为循环是因为他在等待线程被释放后能被重用。

CyclicBarrier 支持一个可选的Runnable 指令并在最后一个线程到达集合点之后且所有线程释放前，每个屏障点执行一次。这个屏障操作在所有同组线程继续执行前更新共享状态非常有用。



